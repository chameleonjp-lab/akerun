# Vault Tumbler Lab — 現行品質監査

## 正本

現行版は React 19 + Vite + native Canvas 2D です。ルールは `shared/akerun/LockMechanism.ts`、金庫・公式問題・報酬は `shared/akerun/GameDefinitions.ts`、描画と入力は `client/src/game/VaultWorld.ts`、画面シェルは `client/src/App.tsx` が担当します。Babylon.js、`/manus-storage/`、固定3輪の記述は現行仕様ではありません。

公式問題は固定カタログ20問（4〜6輪）、訓練は1〜3輪です。通常のseed生成は開発・監査用に限定します。操作履歴は `operationTrace` として決定的に再生し、ブラウザ、リプレイ、SupabaseのV3採点契約が同じ派生値を検証します。

## 監査結果

| 領域            | 現行状態                                                                 | 残課題                                         | 受入確認                                             |
| --------------- | ------------------------------------------------------------------------ | ---------------------------------------------- | ---------------------------------------------------- |
| ルール          | `LockMechanism` がダイヤル、後半アクチュエータ、jam、lockout、openを管理 | 正規ゲートの許容幅やフェンス罰は実機検証が必要 | `pnpm test:rules`、20問の自動開錠                    |
| 公式問題        | 問題ID、輪数、通過、偽ゲート、基準値をカタログ固定                       | 初級05の体感と全問の人間向け時間は未計測       | `pnpm generate:problem-balance`、iPhone実機20問      |
| 採点            | V3で時間・回転差に上限、余分な偽ゲートだけ減点。設定による減点なし       | V3の10倍時間基準を実測値へ置換                 | ブラウザ／リプレイ／SQLの同値確認                    |
| 入力            | タッチ、ドラッグ、ホイール、矢印、精密入力、離脱解除                     | 390×844 / 402×874で長時間操作を確認            | iPhone Safariの15分連続プレイ                        |
| UI/UX           | 進行・訓練・結果・ヘルプを分離。結果で総数と余分な偽ゲートを区別         | タイトルの二次導線とHUDの表示密度を継続調整    | 初回15分の導線テスト                                 |
| 描写            | Canvasが輪数と状態を正本にし、写真は薄い下地                             | 金庫別写真、輪数に応じた下地、達成演出の強化   | 代表状態のスクリーンショット                         |
| 保存/ランキング | 端末保存、Edge入口、決定的リプレイ、V3ランキング関数                     | migration適用後の本番読み取りと受付前試験      | 受付OFF、件数0、旧集計を`hidden`で隔離したことを確認 |

## 採点V3の受入条件

- 機械監査用 `parTime` をそのまま壁時計にせず、暫定の観察時間基準へ展開する。
- 公式20問すべてで、3分クリーンは9,060〜9,440点、12分プレイ（基準超過の失敗1回・余分な偽ゲート20回を含む）は6,020〜7,500点に収まる。
- 30秒程度のクリーンな速度プレイは全20問で10,020点以上になる。
- `ScoreContract.ts`、`replayAkerunTrace.ts`、Edge Function、SQL最終化が、総回転数、余分な回転、観察精度、スコアを同じ値にする。
- V1/V2の過去 `score_runs` と `game_scores` は現行V3ランキングへ混在しない。旧 `game_scores` は既存の `hidden` 状態で隔離する。

## 保留する変更

正規ゲートに幅を与える、偽ゲート接近にedgeを出す、フェンス巻き戻しを初級で無効にする、4アクチュエータの操作プロトコルを差別化する、といった変更は、実機の誤操作率と楽しさを測らずに決めません。ルールの決定的再生を壊さない計測計画を先に作ります。
