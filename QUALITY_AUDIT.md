# 10点到達実装プログラム：現行基盤監査

## 監査時点

現行版は、3枚の固定ホイール、固定のハンドル手順、タイミングラッチ、開扉、宝の達成演出を備える。`LockMechanism` がルール、`VaultWorld` が入力と描画、`AudioFeedback` が音を担う分離は成立している。一方で、ゲーム状態・コンテンツ・難易度・報酬・結果が固定値として各モジュールへ埋め込まれており、10点品質に必要な可変性、初回学習、回帰保証、アクセシビリティを支えるモデルが不足している。

## 現行資産とリスク

| 領域 | 現行資産 | 不足・リスク | 最初の対処 |
|---|---|---|---|
| ルール | 描画非依存の `LockMechanism`、明確なphase | `TUMBLER_STAGES`、ハンドル回数、ラッチ窓が固定値 | `VaultDefinition`、`PuzzleSeed`、`DifficultyProfile` を導入する |
| 入力 | ドラッグ、ホイール、キー、ボタン、Pointer Capture の解除 | 入力補助、感度、キー割当、意味的なアクションのテストがない | 意味的操作の回帰テストと入力設定を追加する |
| 表示 | ダイヤル、断面、因果線、開扉、宝 | 初回の学習導線、失敗理由、現在の次行動が分散している | チュートリアル状態と通知モデルを追加する |
| 難易度 | 固定の3ゲート、ハンドル、ラッチ | 正解を覚えると反復価値が薄い。初見には失敗の意味が難しい | 観察・標準・専門プロファイルとシード生成を追加する |
| 報酬 | 宝のビジュアル | 来歴、収集、結果、次の挑戦がない | `RewardDefinition` とリザルトモデルを追加する |
| 品質保証 | 型検査、ビルド、手作業のデモ、ルール層の単一スクリプト | 状態別、アセット遅延、入力離脱、リセットの継続的回帰が不足 | 純粋ルール層のテスト拡張と描画安全性の品質ゲートを追加する |
| アクセシビリティ | ボタンとキー操作、音のON/OFF | 色・音・モーションの代替、精密操作支援がない | 高コントラスト、字幕、モーション削減、精密モードを設計する |

## 実装順序の決定

1. **データ駆動のルールモデル**を先に作る。これが可変コンビネーション、難易度、報酬、金庫型の共通基盤になる。
2. **ルール層の回帰試験**を増やす。新しい組合せ・難易度を安全に追加するための品質ゲートにする。
3. **チュートリアルと失敗説明**を導入する。可変化によって初見の難しさが増す前に、学習経路を確立する。
4. **リザルトと報酬**を追加する。成功後に再挑戦の動機を残す。
5. **金庫バリエーションと専門モード**は、上記をデータで拡張できる状態になってから追加する。

## フェーズAの受入基準

- 固定値を除去しても、既存の基準金庫は同じ手順で開錠できる。
- 同一seedは同一パズルを再現し、別seedは有効な異なるパズルを生成する。
- 観察・標準・専門の各プロファイルで、手順、猶予、ヒント、ペナルティが規定どおりに切り替わる。
- 不正操作、画像未読込、入力離脱、リセットを含む回帰テストが通る。
- 初回プレイヤーが「現在の目的」「次の操作」「失敗理由」を画面上から読み取れる。
