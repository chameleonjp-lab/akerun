# Game Plan: Vault Tumbler Lab

## Risk Tasks

### 1. ダイヤル入力と刻みへのスナップ
- **Why isolated:** ドラッグ・ホイール・キーボードを同じ 0〜99 の整数回転に変換し、方向が変わっても表示と判定を一致させる必要がある。
- **Approach:** `LockMechanism` が正規化・一刻みずつの移動・方向・解答段階を一元管理する。画面座標からのドラッグ角度は `VaultWorld` 内で刻みに変換し、純粋な状態クラスへ渡す。
- **Verify:** 右矢印／左矢印、ホイール、ダイヤルのドラッグのいずれでも数値が一刻みずつ変わり、時計回り・反時計回りのラベルと動くホイールが一致する。

### 2. 正面ダイヤルと実機風ホイールパックの連動
- **Why isolated:** 単一の数字入力を、そのまま各ホイールへ書き込むのではなく、ドライブカム、フライ、方向反転、通過回数を介して三枚のホイールへ順に伝える必要がある。プレイヤーが「なぜ次に右／左へ反転するか」をカットアウェイから読めなければならない。
- **Approach:** 各段階を「対象番号・必要方向・担当ホイール・通過回数」として定義する。ドライブカムは外側のフライから接続を広げ、方向反転時には現在の輪を残して次の輪へ移る。カットアウェイはドライブカム角、接続中フライ、ゲート、フェンス、ロックボルトを同期して描く。
- **Verify:** 基準金庫では左回りで第三輪を4回通過、右回りで第二輪を3回、左回りで第一輪を2回通過する。異なる契約では開始方向が反転する。接続中のフライだけが青緑に点灯し、反転後は前の輪がその位置に残る。

### 3. 物理的な解除プロトコル
- **Why isolated:** ダイヤル整列だけを終点にせず、テンションの抵抗、フェンスの座り、ボルトの後退という連続的な物理情報へ繋ぐ。固定回数や見えないタイミング待ちではなく、操作の反応からプレイヤーが仮説を検証できなければならない。各段階の噛み込み、表示、音、自動デモも同じ状態機械を読まなければならない。
- **Approach:** `LockMechanism` に「ホイール整列 → テンション帯を保持 → フェンスを座らせる → ボルトを後退」の純粋なプロトコル状態を追加する。過剰な力は回復可能な `jammed` として扱い、難易度に応じて抵抗帯、保持時間、失敗後退、内部遮蔽を変える。`VaultWorld` は状態を単一の物理作業台、抵抗計、フェンス、ボルト、合成金属音へ同期する。
- **Verify:** 3枚のゲートを揃えても、テンション帯、フェンス座り、ボルト後退を満たすまで開かない。過剰な力は明瞭な反動・テキスト・回復操作を返す。標準・専門は未整列ゲートを遮蔽し、`?demo` は全段階を通過して開扉まで到達する。

### 4. データ駆動の金庫定義と品質ゲート
- **Why isolated:** 固定手順のまま金庫・難易度・報酬を追加すると、表示層とルール層へ同じ条件が重複し、調整・回帰・コンテンツ追加が破綻する。
- **Approach:** `VaultDefinition`、`PuzzleSeed`、`DifficultyProfile`、`RewardDefinition`、`ResultSummary` をルール層の設定として定義する。既存の基準金庫を最初の定義として移行し、純粋な回帰試験でseed、難易度、成功、失敗、リセットを検証する。
- **Verify:** 同じseedは同じ手順、異なるseedは有効な異なる手順を生成する。観察・標準・専門でヒント・猶予・ペナルティが変わり、既存の自動デモが基準プロファイルで開錠まで完走する。

## Main Build

Babylon の全画面キャンバス上に、左の大きな正面ダイヤル、右の縦長内部機構、上部計器ストリップ、下部の観察／物理作業台を描く。ダイヤルのドラッグ、マウスホイール、矢印キー、テンション・ハンドル、フェンス・レバー、ボルト・タブ、リセット、自動デモを提供する。`?demo` は決定論的に正解手順を実行し、画面証跡でも機構の遷移と開扉を確認できる。

- **Assets:**
  - `vault_door_plate`（900×900 px）を正面操作盤の金庫フレームとして表示する。
  - `brushed_brass_patina`（1m タイル）をダイヤルとホイールの真鍮面に重ねる。
  - `tumbler_mark`（96×96 px）を画面左上の識別マークとして表示する。
  - `visual_target`（1920×1080 px）を低不透明度の背景参照として表示する。
- **Verify:**
  - マウス、タッチ／ペン、ホイール、キーボード操作のいずれでもダイヤルが反応する。
  - 3枚のホイールの状態、フェンス、ボルト、色付き連動線が常に表示され、重なりや文字切れがない。
  - モバイル縦長ではダイヤルを上、鍵内部を下に積み、同じ情報を読める。
  - 生成アセットに失敗時のプレースホルダーがなく、参照素材がゲームの主要領域に現れる。
  - `?demo` で 3段階のゲート整列、テンション検証、フェンス座り、ボルト後退、開錠状態が順に視認できる。
  - ブラウザコンソールにエラーがなく、`pnpm check` と `pnpm build` が成功する。

## 実機風拡張（2026-08-19）

- **二段階開扉:** フェンス座り後はまずロックボルトを退避させ、次に扉ハンドルでキャリーバーと扉側ロッキングボルトを後退させる。固定回数ではなく、連続する物理移動を成功条件とする。
- **偽ゲート:** 各ホイールに深さの異なる浅い切欠きを置き、観察では橙色の断面、標準以降では反発、ブラインドでは短い減衰音と軽い振動で反証可能な手掛かりを返す。
- **ホイールパック予圧:** Aurora、Nocturne、Pelagic は基準抵抗、フライの粘り、カム縁の硬さを別々に持つ。数値だけでなく回転音、接触音、計測表示で金庫の個性を伝える。
- **観察・保安:** ケースカバー、ブリッジ、ホイールポスト、変更式ホイール、リロッカー、アンチパンチは攻略手順ではなく、分解観察と鑑定帳の安全な資料要素として提供する。
