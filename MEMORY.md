# Development Memory

- ホストは React 19 + Vite の静的プロジェクトで、Babylon は `@babylonjs/core` 9.21.2 を使用する。
- 全画面表示は `DynamicTexture` を貼った Babylon 平面で描く。これにより、ダイヤルとカットアウェイを同じレンダーループで更新できる。
- 生成アセットはプロジェクト外に保持され、以下の永続URLを直接利用する。
  - `/manus-storage/vault-tumbler-reference_35720048.png`
  - `/manus-storage/brushed-brass-patina_3c5701a7.png`
  - `/manus-storage/vault-door-plate_87b42963.png`
  - `/manus-storage/vault-tumbler-mark_c74fcb29.png`
- WebDev の開発用ストレージプロキシにより、`/manus-storage/` を Canvas の `Image` 読込先として使用できる。
- Babylon の平面に DynamicTexture を貼る際は、`vScale = -1` と `vOffset = 1` を設定して、Canvas の上方向と画面上方向を一致させる。
- 実機風アセットを統合したデスクトップ確認では、鋼製扉、切削真鍮ダイヤル、内部ホイールパック、青緑の因果リンクが同時に表示された。モバイルの初回キャプチャはキャンバス初期化前の背景のみになったため、実画面再初期化後にも縦長表示を確認する。
- 再初期化後のモバイル縦長確認では、正面ダイヤル、内部ホイール／フェンス、操作トレイが縦に積まれて表示された。ヘッダーの状態表示はロゴと競合しないよう、縦長時に短い状態名だけを下段右側へ配置する。
- 高速自動デモでは、3枚のゲート整列、ハンドル・インターロック、タイミングラッチ、ボルト退避の後に、扉が蝶番側へ縮退して扉厚と暗い金庫内部が表示される。平面の歪みではなく、閉じた扉の幅を蝶番側から減らし、開口端には独立した厚い金属エッジを描く方式が視覚的に安定した。
- 開錠後の金庫内部には、`vault-treasure-cache` を下側へ配置する。背景の放射状金色・青緑の反射、`screen` 合成で重ねた宝、少数の十字グリントを組み合わせると、暗室の雰囲気を保ったまま金貨・宝石・懐中時計を認識できる。
- デバッグでは、通常画面と高速デモを同一の並列画面捕捉で取得すると高速デモだけ一時的に空白になることがあった。高速デモを単独で取得すると、開錠、開扉、宝、内部機構は正常に描画された。ポインタドラッグは捕捉解除を `pointerup`、`pointercancel`、`lostpointercapture`、window 終了イベントで統一して、画面外での離脱後にも状態を残さない。
- 10点到達版では、`GameDefinitions.ts` が金庫型・報酬・難易度・seedを定義し、`LockMechanism` は可変の `PuzzleDefinition` を受ける。`OBSERVE`、`STANDARD`、`EXPERT`、新規契約、日替わり契約が同じ状態機械を使う。ルール層はVitestでseed、金庫型、報酬、難易度、ロックアウト、開錠を回帰確認する。
- 初回フレームはキャンバス下地として視覚基準アセットを示し、`VaultWorld` が初期描画を直ちに実行する。描画の一時例外はscene境界で隔離され、復旧画面を表示してレンダーループを継続する。高コントラスト、低モーション、精密入力、ガイド、ローカルのみの匿名品質計測をゲーム内で扱う。
- 順次コンテンツ拡張では、`ArchiveLedger` が報酬の解放回数と初回解放日時だけを端末内へ記録し、鑑定帳に来歴・材質・観察メモを表示する。`DailyScorebook` は日替わりseedごとに最短時間、同タイム時の最少失敗数を保存する。分解観察は駆動軸・ホイール群・フェンス・ボルトの4段階で実装し、デスクトップとモバイルの画面で鑑定帳、分解観察、自己ベストの表示を確認した。

## 物理解除フローの検証記録（2026-08-19）

- 標準モードの初期画面では、真鍮ダイヤル、右側カットアウェイ、抵抗計、接触針／候補メモの作業台が同時に表示された。未整列ホイールの正確なゲート位置は `SHIELDED` として隠れ、正解を追従するだけの操作にならないことを確認した。
- `?demo&fast` では、ダイヤル整列、テンション検証、フェンス座り、ボルト後退の順に到達し、扉の蝶番開き、退避済みボルト、収蔵品、開錠結果を確認した。

## 実機風ホイールパックの検証記録（2026-08-19）

- `?demo&fast&difficulty=observe` で、左回り開始の通過回数表示、ドライブカムの回転、外側から順に点灯するフライ、観察モードの因果線を確認した。
- 同デモは、全ホイールの整列、フェンス座り、`LOCK BOLT / RETRACTED`、ボルトワーク解放、蝶番開き、収蔵品表示まで到達した。

## 6輪と聴覚的手掛かりの検証記録（2026-08-19）

- 実ブラウザで、初期画面に6枚のホイール、接続中のフライ、ドライブカム、フェンス、ロックボルト、因果線、通過回数の作業指示が同時に表示されることを確認した。
- 空転、ゲート縁、フライ接続の音色はWeb Audioの初回ユーザー操作後に有効化される。音を無効にしても、作業台の `LISTEN / OFF — VISUAL CUES ACTIVE` とライブ状態文が同じ因果を残す。
- `webdev_take_screenshot` はこのCanvas描画を墨紺の下地だけとして捕捉する場合がある。実ブラウザのCanvas確認を表示品質の根拠とし、デスクトップ実画面での確認を継続する。

## ブラインドモードの検証記録（2026-08-19）

- `?difficulty=blind` で、金庫扉、ダイヤル、6輪ホイールパック、数値、通過回数、抵抗計が完全に暗転し、通常の視覚的解答が露出しないことを確認した。
- `V` キーで視覚補助をオンにすると、黒地のまま最小の状態記号と `BLIND ASSIST / LISTEN` のみが現れ、音なし環境で補助を受けられることを確認した。
- `?demo&fast&difficulty=blind` は暗転状態のまま開始する。6輪・27通過の手順は従来の3輪デモより長く、初回10秒の確認時点では途中だったため、完走検証は十分な待機時間で実施する。
- 追加待機後、同デモが暗転状態のまま `LOCK BOLT RELEASED` を表示し、内部の解答を露出せずに開錠成功へ到達することを確認した。

## モバイル・ハプティックの検証記録（2026-08-19）

- `HapticFeedback` はVibration APIの有無を遅延検出し、最初のポインター、ホイール、またはキーボード操作後だけ振動を開始する。空転、ゲート縁、フライ接続、整列、テンション、フェンス座り、噛み込み、開錠成功は別の短いパターンを持つ。
- 低モーションを有効にすると振動を即時キャンセルし、`K` または作業台の `HAPTIC` から利用者が明示的に切り替えられる。Vibration API非対応環境は例外を出さず `HAPTIC / N/A` と表示する。
- 単体試験で対応・低モーション停止・非対応時の無操作を確認し、ルール回帰は8件へ拡張した。
- 実ブラウザの通常画面では、既存の6輪カットアウェイと操作列を維持したまま `HAPTIC / ON` が作業台に表示されることを確認した。ブラウザ自動操作セッションはキー送信時に `about:blank` へ遷移する場合があるため、切替の挙動は単体試験とCanvas内のクリック導線でも保証する。

## 扉ボルトワークの検証記録（2026-08-19）

- `?demo&fast&difficulty=observe` の実画面で、6輪カットアウェイ、段階的なフライ接続、ロックボルト、扉側ボルトのラベルが初期および中間進行中に表示されることを確認した。自動デモは6輪・通過回数が多いため、開扉の最終状態は追加待機で確認する。
- 追加待機後、同デモが `LOCK BOLT / RETRACTED`、青緑のキャリーバー、`DOOR BOLTS / RETRACTED`、開扉、収蔵品表示まで到達することを確認した。観察モード初期画面では、正規ゲートとは別の橙色で浅い偽ゲートが6輪に表示されることも確認した。

## 偽ゲート訓練・観察メモの検証記録（2026-08-20）

- `?training=false-gate` で、`FALSE-GATE PRACTICUM` の二輪カットアウェイ、橙の浅い偽ゲート、訓練計器ラベルが表示されることを確認した。固定の6輪ラベルは訓練用に `TWO WHEEL PACK` へ切り替える。
- Jキーで現在の接触を匿名の端末内メモとして保存でき、保存後に `NOTES / O` で閲覧できる状態文へ切り替わることを確認した。メモは外部送信しない。
